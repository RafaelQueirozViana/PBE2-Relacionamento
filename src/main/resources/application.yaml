spring:
  application:
    name: recanto-suave
  datasource:
    url: jdbc:h2:mem:recanto
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
